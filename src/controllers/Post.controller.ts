import {Request, Response} from "express";
// import { Model } from "../someModel"

export const getPosts = async (request: Request, Response: Response) => {
    // const posts = await getRepository(Entity)
    // posts.create()
}